# mpe.md — REDIRECIONADOR DE COMPATIBILIDADE

Data: 07/08/2026
Estado: SUPERADO COMO FONTE AUTÔNOMA

Este arquivo existe apenas para preservar links e rotas antigas.

A definição vigente de `/mpe`, `/mpe+` e `/mpe-` está em **`comandos.md` v1.7.0 ou posterior**.

Regras mínimas de compatibilidade:
- `/mpe` = Melhore o Prompt e Execute-o;
- `/mpe+` = mais informações + execução;
- `/mpe-` = menos informações + execução;
- os três geram cabeçalho automaticamente;
- **não é necessário `/id`**;
- somente `/id off` suprime o cabeçalho quando solicitado;
- se este arquivo divergir de `comandos.md`, prevalece `comandos.md`.

Não reutilizar definições históricas anteriores deste arquivo como regra ativa.
